START paycheck_calculator

    GET hours_worked

    IF hours_worked > 40
        LET regular_pay = 40 * 20
        LET overtime_hours = hours_worked - 40
        LET overtime_pay = overtime_hours * 30
        LET paycheck = regular_pay + overtime_pay
    ELSE
        LET paycheck = hours_worked * 20
    ENDIF

    PUT paycheck

END paycheck_calculator

# === References ===
# Southern New Hampshire University. IT-140 Module Three Assignment: Introduction to Pseudocode and Flowcharts.
# Lucius L. H. Hands-on Python Tutorial. Python if/else and weekly wage calculation.
